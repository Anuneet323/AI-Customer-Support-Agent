# Prompts Directory

This directory stores versioned prompt templates for:
- Intent classification (`v1`)
- Grounded reply generation (`v1`)
- Rubric-based LLM judge (`v1`)

Default templates are loaded from `src/generation/reply_generator.py` and `evaluation/judge.py`.
